<%- include("partials/header") %>

<h2>Quotes Found</h2>

<% if (quotes.length === 0) { %>
  <p>No quotes found for your search.</p>
<% } else { %>
  <ul>
    <% quotes.forEach(q => { %>
      <li>"<%= q.quote %>" — <%= q.firstName %> <%= q.lastName %> (<%= q.category %>)</li>
    <% }) %>
  </ul>
<% } %>

<%- include("partials/footer") %>